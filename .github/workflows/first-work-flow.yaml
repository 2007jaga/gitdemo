name : First WorkFlow
on : [pull_request,push]
jobs: 
    run-shell-commands: 
        runs-on: ubuntu-latest
        environment: test
        steps: 
             
            - name : "envvariablevalue"
              run: echo ${{vars.TEST_VARIABLE}}
